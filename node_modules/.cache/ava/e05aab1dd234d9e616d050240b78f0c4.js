'use strict';

const test = require('ava');
const mimic = require('./index');

test('main', t => {

    const sayCheese = () => {
        say: "Cheese";
    };

    function wrap() {
        return sayCheese;
    }
    mimic(wrap, sayCheese);
    console.log(wrap.say);

    //t.is(wrap.name, sayCheese.name);
    t.is(wrap.say, sayCheese.say);
});
//# sourceMappingURL=/Volumes/Resource/New Folder With Items/Programming/Obboy/mimicking-func/node_modules/.cache/ava/e05aab1dd234d9e616d050240b78f0c4.js.map